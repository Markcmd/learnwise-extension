import { describe, it, expect } from "vitest";

// The one "scaffolding works" sample test from task 0.1.
describe("scaffolding", () => {
  it("runs the Vitest runner", () => {
    expect(1 + 1).toBe(2);
  });
});
